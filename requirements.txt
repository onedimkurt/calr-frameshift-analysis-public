# Python 3.10.14
# Install via conda (recommended): conda env create -f environment.yml
# Or via pip into a Python 3.10 environment:
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.5.1
biopython==1.87
localcider==0.1.21
matplotlib>=3.7,<3.9

# Optional — only for the sequence-ensemble analysis (04_review_validation/C10_albatross.py):
# sparrow  (ALBATROSS)  — see https://github.com/idptools/sparrow for installation
